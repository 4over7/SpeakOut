name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# 同一分支有新 push 时取消旧的运行，节省额度
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ============================================================
  # macOS: 分析 + 测试 + 编译 (主平台)
  # ============================================================
  macos:
    runs-on: macos-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Static analysis
        run: flutter analyze

      - name: Run tests
        run: flutter test

      - name: Compile native library
        run: |
          cd native_lib
          clang -dynamiclib \
            -framework Cocoa -framework Carbon \
            -framework AVFoundation -framework AudioToolbox \
            -framework CoreAudio -framework Accelerate \
            -o libnative_input.dylib native_input.m -fobjc-arc

      - name: Build macOS app
        run: flutter build macos --release

  # ============================================================
  # Windows: 分析 + 测试 + 编译原生库 + 编译 Flutter
  # ============================================================
  windows:
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Static analysis
        run: flutter analyze

      - name: Run tests
        run: flutter test

      - name: Compile native library (MSVC)
        shell: cmd
        run: |
          cd native_lib\windows
          mkdir build
          cd build
          cmake .. -G "Visual Studio 17 2022" -A x64
          cmake --build . --config Release

      - name: Build Windows app
        run: flutter build windows --release

  # ============================================================
  # Linux: 分析 + 测试 (编译待 Phase 2 完成)
  # ============================================================
  linux:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            clang cmake ninja-build pkg-config \
            libgtk-3-dev liblzma-dev libstdc++-12-dev

      - name: Install dependencies
        run: flutter pub get

      - name: Static analysis
        run: flutter analyze

      - name: Run tests
        run: flutter test
