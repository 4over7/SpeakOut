{
    "name": "speakout-gateway",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "test": "node --test",
        "deploy": "wrangler deploy",
        "dev": "wrangler dev",
        "start": "wrangler dev"
    },
    "dependencies": {
        "hono": "^4.0.0"
    },
    "devDependencies": {
        "wrangler": "^3.0.0"
    }
}
